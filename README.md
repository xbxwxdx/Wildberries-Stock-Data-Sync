### Описание программы

Эта Python-программа предназначена для автоматизации процесса сбора и сохранения данных об остатках товаров на маркетплейсе Wildberries. Она получает доступ к API Wildberries с использованием аутентификационного токена, запрашивает актуальные остатки товаров и сохраняет полученные данные в базу данных SQLite.

#### Основные шаги программы:

1. **Загрузка настроек**: Токен API загружается из переменной окружения (`os.getenv`) или задаётся вручную.
   
2. **Получение текущих данных**: Использует библиотеку `requests` для отправки GET-запроса к API Wildberries с указанием текущего дня в качестве начальной даты (`dateFrom`).

3. **Обработка JSON-данных**: Полученный ответ преобразуется в объект JSON и сохраняется в Pandas DataFrame.

4. **Создание базы данных**: Создаёт файл базы данных SQLite с именем `my_database1.db`, если таковой отсутствует, и создаёт таблицу `supplier_stocks`, если таблица ещё не существует.

5. **Запись данных**: Данные из DataFrame записываются в таблицу базы данных с помощью метода `.to_sql()` библиотеки Pandas.

6. **Закрытие соединения**: После записи данных база данных закрывается, а изменения сохраняются.

Таким образом, эта программа позволяет автоматически получать и сохранять ежедневные остатки товаров поставщика на платформе Wildberries в локальной базе данных SQLite.


# Почему лучше выполнять скрипт на удалённом сервере (Ubuntu) по расписанию

При автоматизации регулярных задач, таких как выгрузка отчётов из Wildberries и отправка по почте, возникает вопрос: где запускать скрипт? Можно настроить его на локальном компьютере, но использование удалённого сервера (например, хостинг Beget с Ubuntu) даёт множество преимуществ.

## 1. Круглосуточная доступность (24/7)
Сервер работает постоянно, не выключается и не перезагружается без необходимости. Локальный компьютер может быть выключен на ночь, в поездке или из-за сбоя питания. Если скрипт должен выполняться каждый день в определённое время (например, утром для отправки отчёта), сервер гарантирует, что задача будет запущена независимо от состояния вашего рабочего ПК.

## 2. Автоматизация и планировщик cron
На сервере под управлением Ubuntu есть встроенный планировщик задач **cron**. Он идеально подходит для выполнения скриптов по расписанию:
- Гибкая настройка периодичности (каждый день, каждый час, по будням и т.д.).
- Логирование вывода для контроля выполнения.
- Не требует ручного запуска или напоминаний.
- Можно легко изменить расписание через crontab.

## 3. Независимость от локального интернета и железа
Сервер размещён в дата-центре с надёжным интернет-каналом и резервированием питания. Даже если дома пропадёт свет или интернет, скрипт всё равно выполнится. Локальный запуск зависит от вашего провайдера и стабильности сети.

## 4. Централизованное хранение данных и логов
На сервере можно настроить единое хранилище для всех генерируемых отчётов, логов выполнения и базы данных (если используется). Это удобно для последующего анализа, а также для доступа с разных устройств. Локальные файлы часто теряются или засоряют диск.

## 5. Масштабируемость
Если в будущем потребуется добавить новые скрипты, обрабатывающие другие данные (например, остатки, продажи, заказы), сервер легко справится с дополнительной нагрузкой. Можно организовать целую систему скриптов с общим окружением и библиотеками.

## 6. Безопасность и изоляция
На сервере можно создать отдельного пользователя для задач, ограничить права доступа, хранить секреты (токены, пароли) в защищённых файлах `.env` с правами 600. Это снижает риск случайной утечки данных, тогда как на локальном компьютере файлы могут быть доступны другим пользователям или программам.

## 7. Экономия ресурсов локального компьютера
Скрипты, работающие с API и формирующие Excel, могут потреблять заметные ресурсы (память, процессор). На сервере это не мешает вашей основной работе. Вы не тратите электричество на круглосуточную работу домашнего ПК.

## 8. Мониторинг и оповещения
На сервере можно настроить дополнительный мониторинг выполнения скриптов (например, через email‑уведомления о сбоях, интеграцию с Telegram‑ботами). Если задача не выполнилась, вы узнаете об этом сразу. Локальный компьютер не всегда может отправить уведомление, если он выключен.

## 9. Пример: хостинг Beget
Beget предоставляет недорогие тарифы с поддержкой SSH, доступом к cron и достаточными ресурсами для небольших автоматизаций.

Настройка:
- Загружаете скрипт и зависимости.
- Создаёте виртуальное окружение (опционально).
- Прописываете задание в crontab через команду `crontab -e`.
- Логируете вывод в файл для отладки.
- Всё работает без вашего участия.

Использование удалённого сервера (Ubuntu) с планировщиком cron – это стандарт индустрии для автоматизации рутинных задач. Это надёжно, безопасно и не требует вашего вмешательства после первоначальной настройки. Вложения в хостинг окупаются спокойствием и стабильностью работы ваших скриптов.

## Выбор хостинга и контактная информация

Для запуска скрипта на удалённом сервере рекомендую использовать проверенный хостинг **Beget**. Они предлагают гибкие тарифы, отличную техническую поддержку и удобный доступ по SSH.

### Рекомендуемый тариф (VPS)

Минимальной конфигурации достаточно для работы скрипта:

- **1 ядро CPU**
- **1 ГБ RAM**
- **10 ГБ NVMe** (быстрый диск)
- **Канал 250 Мб/сек**

👉 [Перейти к выбору тарифа VPS на Beget](https://beget.com/p2188013/ru/vps#vps-plans-list)  
*(партнёрская ссылка)*

### Почему этого достаточно?

Скрипт выполняется несколько секунд раз в сутки, потребляя минимум ресурсов. Даже самого дешёвого тарифа хватит с запасом. Вы также сможете разместить на сервере другие автоматизации или небольшие веб-проекты.

---


### Контакты

Для вопросов и предложений:  
xbxwxdx@gmail.com

